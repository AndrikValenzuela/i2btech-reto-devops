apiVersion: v1
kind: Service
metadata:
  name: {{ include "i2btech-app.fullname" . }}
  labels:
    {{- include "i2btech-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "i2btech-app.selectorLabels" . | nindent 4 }}
